---
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: default-deny
  labels:
    team: platform
    env: production
spec:
  description: "Default deny all ingress and egress for all pods; explicit allow policies required elsewhere"
  endpointSelector: {}
  ingress: []
  egress: []
