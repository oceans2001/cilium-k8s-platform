apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: cilium-platform
nodes:
- role: control-plane
- role: worker
- role: worker
networking:
  disableDefaultCNI: true        # Cilium will be the CNI
  podSubnet: "10.0.0.0/16"      # Pod IP range
  serviceSubnet: "10.96.0.0/16" # Service IP range
