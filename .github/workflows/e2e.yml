---
name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    name: Kind + Cilium + Argo CD
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.23.0/kind-linux-amd64
          chmod +x ./kind && sudo mv ./kind /usr/local/bin/kind

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Create Kind cluster
        run: kind create cluster --config platform/kind/cluster-config.yaml --wait 60s

      - name: Install Gateway API CRDs
        run: |
          kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v1.2.0/config/crd/standard/gateway.networking.k8s.io_gatewayclasses.yaml
          kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v1.2.0/config/crd/standard/gateway.networking.k8s.io_gateways.yaml
          kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v1.2.0/config/crd/standard/gateway.networking.k8s.io_httproutes.yaml
          kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v1.2.0/config/crd/standard/gateway.networking.k8s.io_referencegrants.yaml
          kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v1.2.0/config/crd/standard/gateway.networking.k8s.io_grpcroutes.yaml

      - name: Install Cilium
        run: |
          helm repo add cilium https://helm.cilium.io/
          helm repo update
          helm install cilium cilium/cilium \
            --namespace kube-system \
            --values platform/cilium/values.yaml \
            --set lbIPAM.enabled=false \
            --wait \
            --timeout 5m

      - name: Wait for Cilium to be ready
        run: |
          kubectl -n kube-system rollout status daemonset/cilium --timeout=3m
          kubectl -n kube-system rollout status deployment/cilium-operator --timeout=2m

      - name: Install MetalLB
        run: |
          helm repo add metallb https://metallb.github.io/metallb
          helm repo update
          helm install metallb metallb/metallb \
            --namespace metallb-system \
            --create-namespace \
            --wait \
            --timeout 3m

      - name: Configure MetalLB IP pool
        run: |
          BASE=$(docker network inspect kind \
            | python3 -c "
          import sys, json
          networks = json.load(sys.stdin)[0]['IPAM']['Config']
          ipv4 = [n for n in networks if ':' not in n['Subnet']]
          print(ipv4[0]['Subnet'].rsplit('.', 1)[0])
          ")
          echo "Using IP base: $BASE"
          kubectl apply -f - <<YAML
          apiVersion: metallb.io/v1beta1
          kind: IPAddressPool
          metadata:
            name: kind-pool
            namespace: metallb-system
          spec:
            addresses:
              - ${BASE}.200-${BASE}.250
          ---
          apiVersion: metallb.io/v1beta1
          kind: L2Advertisement
          metadata:
            name: kind-l2
            namespace: metallb-system
          YAML

      - name: Create self-signed TLS secret
        run: |
          kubectl create namespace gateway-infra
          openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
            -keyout /tmp/tls.key \
            -out /tmp/tls.crt \
            -subj "/CN=example.com"
          kubectl create secret tls platform-tls \
            --namespace gateway-infra \
            --cert=/tmp/tls.crt \
            --key=/tmp/tls.key

      - name: Install Argo CD
        run: |
          kubectl create namespace argocd
          kubectl apply -n argocd \
            --server-side \
            -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
          kubectl -n argocd rollout status deployment/argocd-server --timeout=3m

      - name: Apply App of Apps
        run: |
          # In PRs, point Argo CD at this branch instead of main so the
          # manifests being tested are the ones actually being reviewed.
          BRANCH="${{ github.head_ref || github.ref_name }}"
          echo "Resolved branch from GitHub context: $BRANCH"
          find platform/argocd -name "*.yaml" -exec sed -i "s|targetRevision: HEAD|targetRevision: $BRANCH|" {} \;
          # NOTE: we intentionally do NOT apply root-app.yaml here.
          # root's own sync would recreate these three Applications from
          # Git with targetRevision: HEAD, undoing our branch patch above
          # (App-of-Apps always tracks main; testing a PR branch through
          # it needs a PR-aware generator, which is out of scope for CI).
          kubectl apply -f platform/argocd/apps/gateway-infra.yaml
          kubectl apply -f platform/argocd/apps/platform-policies.yaml
          kubectl apply -f platform/argocd/apps/quote-service.yaml

      - name: Wait for quote-service to sync
        run: |
          echo "Waiting for quote-app namespace..."
          for i in $(seq 1 24); do
            if kubectl get namespace quote-app 2>/dev/null; then
              echo "Namespace found"
              break
            fi
            echo "Attempt $i/24 - waiting 10s..."
            sleep 10
          done
          kubectl get namespace quote-app || { echo "Namespace never created"; exit 1; }
          kubectl -n quote-app rollout status deployment/quote-service --timeout=3m

      - name: Wait for Gateway IP
        run: |
          echo "Waiting for Gateway IP..."
          for i in $(seq 1 18); do
            GW_IP=$(kubectl get gateway -n gateway-infra shared-gateway \
              -o jsonpath='{.status.addresses[0].value}' 2>/dev/null || echo "")
            if [ -n "$GW_IP" ]; then
              echo "Gateway IP: $GW_IP"
              echo "GW_IP=$GW_IP" >> $GITHUB_ENV
              break
            fi
            echo "Attempt $i/18 - no IP yet, waiting 10s..."
            sleep 10
          done
          if [ -z "$GW_IP" ]; then
            echo "Gateway never got an IP"
            exit 1
          fi

      - name: Test quote-service via Gateway
        run: |
          echo "Testing via Gateway IP: $GW_IP"
          kubectl run curl-test \
            --image=curlimages/curl:latest \
            --restart=Never \
            --namespace=default \
            -- curl -s http://$GW_IP/quote
          kubectl wait --for=jsonpath='{.status.phase}'=Succeeded pod/curl-test --timeout=30s || true
          OUTPUT=$(kubectl logs curl-test 2>/dev/null || echo "")
          kubectl delete pod curl-test --ignore-not-found || true
          echo "Response: $OUTPUT"
          if echo "$OUTPUT" | grep -q "quote"; then
            echo "✅ quote-service is healthy via Gateway"
          else
            echo "❌ Expected quote in response, got: $OUTPUT"
            kubectl -n quote-app logs -l app=quote-service --tail=20
            exit 1
          fi

      - name: Test zero-trust network policy
        run: |
          echo "=== Verifying default-deny blocks direct pod-to-pod traffic ==="
          SVC_IP=$(kubectl get svc -n quote-app quote-service -o jsonpath='{.spec.clusterIP}')
          echo "quote-service ClusterIP: $SVC_IP"
          kubectl run policy-test \
            --image=curlimages/curl:latest \
            --restart=Never \
            --namespace=default \
            -- curl -s --max-time 5 http://$SVC_IP:80/quote
          kubectl wait --for=jsonpath='{.status.phase}'=Succeeded pod/policy-test --timeout=30s || true
          DIRECT_OUTPUT=$(kubectl logs policy-test 2>/dev/null || echo "blocked")
          kubectl delete pod policy-test --ignore-not-found || true
          echo "Direct access result: $DIRECT_OUTPUT"
          if echo "$DIRECT_OUTPUT" | grep -q "quote"; then
            echo "❌ Direct pod access should be blocked but got a response"
            exit 1
          fi
          echo "✅ Zero-trust verified: direct pod access is blocked"

      - name: Debug info on failure
        if: failure()
        run: |
          echo "=== Pods ==="
          kubectl get pods -A
          echo "=== Gateways ==="
          kubectl get gateway -A
          echo "=== HTTPRoutes ==="
          kubectl get httproute -A
          echo "=== CNP ==="
          kubectl get cnp -n quote-app -o yaml || true
          echo "=== CCNP ==="
          kubectl get ccnp -o yaml || true
          echo "=== Argo CD Apps ==="
          kubectl get applications -n argocd || true
          echo "=== quote-service logs ==="
          kubectl -n quote-app logs -l app=quote-service --tail=50 || true
