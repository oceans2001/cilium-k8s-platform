---
name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    name: Kind + Cilium + Argo CD
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4.2.2

      # --- tooling ---
      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.23.0/kind-linux-amd64
          chmod +x ./kind && sudo mv ./kind /usr/local/bin/kind

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Install Argo CD CLI
        run: |
          curl -sSL -o /usr/local/bin/argocd \
            https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      # --- cluster ---
      - name: Create Kind cluster
        run: kind create cluster --config platform/kind/cluster-config.yaml --wait 60s

      # --- cilium ---
      - name: Install Cilium
        run: |
          helm repo add cilium https://helm.cilium.io/
          helm repo update
          helm install cilium cilium/cilium \
            --namespace kube-system \
            --values platform/cilium/values.yaml \
            --wait \
            --timeout 5m

      - name: Wait for Cilium to be ready
        run: |
          kubectl -n kube-system rollout status daemonset/cilium --timeout=3m
          kubectl -n kube-system rollout status deployment/cilium-operator --timeout=2m

      # --- argo cd ---
      - name: Install Argo CD
        run: |
          kubectl create namespace argocd
          kubectl apply -n argocd \
	    --server-side \
            -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
          kubectl -n argocd rollout status deployment/argocd-server --timeout=3m

      - name: Apply App of Apps
        run: |
          kubectl apply -f platform/argocd/app-of-apps.yaml

      # --- wait for sync ---
      - name: Wait for quote-service to sync
        run: |
          # дать Argo CD время обнаружить и применить манифесты
          sleep 30
          kubectl -n quote-app rollout status deployment/quote-service --timeout=3m

      # --- test ---
      - name: Test quote-service endpoint
        run: |
          kubectl -n quote-app port-forward svc/quote-service 8080:80 &
          sleep 3
          response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/quote)
          echo "HTTP status: $response"
          if [ "$response" != "200" ]; then
            echo "❌ Expected 200, got $response"
            kubectl -n quote-app logs -l app=quote-service --tail=50
            exit 1
          fi
          echo "✅ quote-service is healthy"

      # --- debug on failure ---
      - name: Debug info on failure
        if: failure()
        run: |
          echo "=== Pods ==="
          kubectl get pods -A
          echo "=== Argo CD Apps ==="
          kubectl get applications -n argocd
          echo "=== quote-service logs ==="
          kubectl -n quote-app logs -l app=quote-service --tail=100 || true
          echo "=== Events ==="
          kubectl get events -n quote-app --sort-by='.lastTimestamp'
